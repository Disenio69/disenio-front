---
interface Props {
	title: string;
}

const { title } = Astro.props;
import '../css/style.css';
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/png" href="../../public/img/favicon.png">
	    <link href='https://fonts.googleapis.com/css?family=Lato:400,700' rel='stylesheet' type='text/css'>
		<script src="https://kit.fontawesome.com/aef010b255.js" crossorigin="anonymous"></script>
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="flex justify-center bg-gradient-to-br from-blue-100 via-sky-100 to-white min-h-screen">
		<div class="w-full max-w-7xl">
			<nav class="bg-white border-b shadow-sm flex flex-wrap items-center justify-between" navbar-main navbar-scroll="false">   
				<div class="flex items-center justify-between w-full px-4 py-1 mx-auto flex-wrap-inherit">
					<div>
						<img src="../../public/img/school-icon.png" alt="school icon" class="w-10 ml-5">
					</div>
					<span class="ml-5 font-bold">Aula virtual EFP N°31</span>
					<!--left options -->
					<div class="flex items-center mt-2 grow sm:mt-0 sm:mr-6 md:mr-0 lg:flex lg:basis-auto">
						<ul class="md:ml-auto md:pr-4 flex flex-row justify-end pl-0 mb-0 list-none md-max:w-full">
							<li id="dashboard" class="flex items-center hidden">
								<a href="/inicio-dashboard" class="flex relative text-black ml-10 underline hover:text-blue-700">
									Dashboard
								</a>
							</li>
							<li class="flex items-center">
							<div id="dropdownButton" class="py-1 px-2 flex relative rounded-full text-white ml-10 bg-gray-800 hover:bg-gray-700 transition">
								<button id="buttonText" class="ml-2 mr-3">Juan riquelme</button>
								<div id="dropdownContent" class="dropdown-content hidden absolute bg-white w-full shadow-lg rounded-sm border-2 left-0 z-10 text-black">
									<a href="">Perfil</a>
									<a href="">cerrar sesión</a>
								</div>
								<img src="../../public/img/usuario-de-perfil.png" alt="" class="w-10">
							</div>
							</li>
						</ul>
						<!-- FIN Sign in Menu(flex) config notificacion box -->
					</div>
				</div>
			</nav>
			<div class="flex justify-center bg-gradient-to-b from-gray-200 to-white min-h-screen">
				<div class="w-full max-w-[1200px] flex flex-col items-center px-4 py-8 space-y-6">

					<!-- Slot u otros componentes -->
					<slot />

				</div>
			</div>
		</div>
	</body>
</html>

<script>
	//@ts-nocheck
	import { jwtDecode } from 'jwt-decode'

	interface Payload {
		tipo: string
		correo: string
		nombre: string
		apellido: string
		id: number
		iat: number
		exp: number
		rol?: string
	}

	if (typeof window !== 'undefined') {
		const token = localStorage.getItem('token')

		if (!token) {
		window.location.href = '/error'
		} else {
		try {
			const payload = jwtDecode<Payload>(token)

			if (!['profesor', 'administrador', 'alumno'].includes(payload.ocupacion.toLowerCase())) {
				window.location.href = '/error'
			}
			if(payload.ocupacion === 'profesor' ){
				document.getElementById('dashboard')?.classList.remove('hidden')
			}


		} catch (e) {
			window.location.href = '/'
		}
		}

		
	}

	


	//MENU DESPLEGABLE
    const  drop = document.querySelector("#dropdownButton")
    const dropmenu = document.querySelector("#dropdownContent")
    drop?.addEventListener("click",()=>{
        if(dropmenu?.classList.contains("hidden")){
            dropmenu?.classList.remove("hidden")
        }
        else
		{
			dropmenu?.classList.add("hidden")
		}
        })
</script>

<style>
	.dropdown-content a {
	  color: black;
	  padding: 12px 16px;
	  text-decoration: none;
	  display: block;
	}
	
	.dropdown-content a:hover {
	  background-color: #ddd;
	}
	</style>